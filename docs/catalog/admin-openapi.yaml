components:
  schemas:
    CandidateBucketCount:
      additionalProperties: false
      properties:
        count:
          format: int64
          type: integer
        entity_type:
          format: int32
          type: integer
        status:
          format: int32
          type: integer
      required:
        - entity_type
        - status
        - count
      type: object
    CandidateItem:
      additionalProperties: false
      properties:
        a:
          $ref: "#/components/schemas/EntitySummary"
        a_id:
          format: int64
          type: integer
        b:
          $ref: "#/components/schemas/EntitySummary"
        b_id:
          format: int64
          type: integer
        created_at:
          format: date-time
          type: string
        decided_at:
          format: date-time
          type:
            - string
            - "null"
        decided_by:
          format: int64
          type:
            - integer
            - "null"
        entity_type:
          format: int32
          type: integer
        reason:
          format: int32
          type: integer
        score:
          format: float
          type:
            - number
            - "null"
        status:
          format: int32
          type: integer
      required:
        - a
        - b
        - entity_type
        - a_id
        - b_id
        - reason
        - score
        - status
        - created_at
        - decided_by
        - decided_at
      type: object
    CatalogSource:
      additionalProperties: false
      properties:
        id:
          format: int32
          type: integer
        is_deprecated:
          type: boolean
        key:
          type: string
        note:
          type: string
        trust_tier:
          format: int32
          type: integer
        url_template:
          type:
            - string
            - "null"
      required:
        - id
        - key
        - url_template
        - trust_tier
        - note
        - is_deprecated
      type: object
    ClaimActionResult:
      additionalProperties: false
      properties:
        event_id:
          format: int64
          type: integer
        from_state:
          type:
            - string
            - "null"
        to_state:
          type: string
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - from_state
        - to_state
        - event_id
      type: object
    DecideCandidateData:
      additionalProperties: false
      properties:
        decided:
          type: boolean
        needs_manual:
          type: boolean
        person_created:
          type: boolean
        person_id:
          format: int64
          type: integer
        proposal_id:
          description: Set when a merge-candidate accept opened a proposal
          format: int64
          type: integer
        released:
          items:
            format: int64
            type: integer
          type:
            - array
            - "null"
      required:
        - decided
      type: object
    DecideCandidateInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/DecideCandidateInputBody.json
          format: uri
          readOnly: true
          type: string
        a_id:
          format: int64
          type: integer
        action:
          enum:
            - accept
            - reject
            - defer
          type: string
        b_id:
          format: int64
          type: integer
        entity_type:
          format: int32
          type: integer
        note:
          description: "merge accept only: carried onto the opened proposal"
          type: string
        source_id:
          description: "merge accept only (non-credit_name): the entity absorbed by the merge (must be a_id or b_id)"
          format: int64
          type: integer
        target_id:
          description: "merge accept only (non-credit_name): the surviving entity"
          format: int64
          type: integer
      required:
        - entity_type
        - a_id
        - b_id
        - action
      type: object
    DetachImageReferencesData:
      additionalProperties: false
      properties:
        removed:
          additionalProperties:
            format: int64
            type: integer
          description: Rows released per kind
          type: object
        total_removed:
          format: int64
          type: integer
      required:
        - removed
        - total_removed
      type: object
    DetachImageReferencesInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/DetachImageReferencesInputBody.json
          format: uri
          readOnly: true
          type: string
        hash:
          description: The image's sha-256 (64 hex characters)
          type: string
      required:
        - hash
      type: object
    DetachNameData:
      additionalProperties: false
      properties:
        detached:
          type: boolean
      required:
        - detached
      type: object
    DetachNameInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/DetachNameInputBody.json
          format: uri
          readOnly: true
          type: string
        credit_name_id:
          description: The credit name to detach from its person (person_id → NULL); an emptied auto-linked person is removed
          format: int64
          minimum: 1
          type: integer
      required:
        - credit_name_id
      type: object
    EntityRefSummary:
      additionalProperties: false
      properties:
        external_id:
          type: string
        link_kind:
          format: int32
          type: integer
        source_id:
          format: int32
          type: integer
      required:
        - source_id
        - external_id
        - link_kind
      type: object
    EntitySummary:
      additionalProperties: false
      properties:
        claim_state:
          format: int32
          type: integer
        content_rating:
          format: int32
          type: integer
        credit_count:
          format: int64
          type: integer
        display_name:
          type: string
        id:
          format: int64
          type: integer
        medium_id:
          format: int32
          type: integer
        olang:
          type: string
        refs:
          items:
            $ref: "#/components/schemas/EntityRefSummary"
          type:
            - array
            - "null"
        release_year:
          format: int32
          type: integer
        site:
          type: string
        source_id:
          format: int32
          type: integer
        work_status:
          format: int32
          type: integer
      required:
        - id
        - display_name
      type: object
    EnvelopeClaimActionResult:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeClaimActionResult.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/ClaimActionResult"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeDecideCandidateData:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeDecideCandidateData.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/DecideCandidateData"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeDetachImageReferencesData:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeDetachImageReferencesData.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/DetachImageReferencesData"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeDetachNameData:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeDetachNameData.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/DetachNameData"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeImageReferencesData:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeImageReferencesData.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/ImageReferencesData"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopePageCandidateItem:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopePageCandidateItem.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/PageCandidateItem"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopePagePendingClaimItem:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopePagePendingClaimItem.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/PagePendingClaimItem"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopePageProbableRefItem:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopePageProbableRefItem.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/PageProbableRefItem"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopePageProposalItem:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopePageProposalItem.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/PageProposalItem"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeProposalActionData:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeProposalActionData.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/ProposalActionData"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeQueueSummary:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeQueueSummary.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/QueueSummary"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeRefActionData:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeRefActionData.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/RefActionData"
        message:
          type: string
      required:
        - code
        - message
      type: object
    EnvelopeReleaseWorkData:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/EnvelopeReleaseWorkData.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data:
          $ref: "#/components/schemas/ReleaseWorkData"
        message:
          type: string
      required:
        - code
        - message
      type: object
    HouseError:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/HouseError.json
          format: uri
          readOnly: true
          type: string
        code:
          format: int64
          type: integer
        data: {}
        message:
          type: string
      required:
        - code
        - message
      type: object
    ImageReferenceItem:
      additionalProperties: false
      properties:
        entity_id:
          description: The owning work / character / label / person
          format: int64
          type: integer
        kind:
          description: work_cover / work_screenshot / character_bust / character_bust_source / character_figure / character_figure_source / label_logo / person_photo
          type: string
        label:
          description: The owning entity's display name
          type: string
      required:
        - kind
        - entity_id
        - label
      type: object
    ImageReferencesData:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/ImageReferenceItem"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    PageCandidateItem:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/CandidateItem"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    PagePendingClaimItem:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/PendingClaimItem"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    PageProbableRefItem:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/ProbableRefItem"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    PageProposalItem:
      additionalProperties: false
      properties:
        items:
          items:
            $ref: "#/components/schemas/ProposalItem"
          type:
            - array
            - "null"
        total:
          format: int64
          type: integer
      required:
        - items
        - total
      type: object
    PendingClaimItem:
      additionalProperties: false
      properties:
        display_name:
          type: string
        product_work_id:
          format: int64
          type:
            - integer
            - "null"
        site:
          type:
            - string
            - "null"
        submitted_event_id:
          format: int64
          type:
            - integer
            - "null"
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - display_name
        - site
        - product_work_id
        - submitted_event_id
      type: object
    ProbableRefBucketCount:
      additionalProperties: false
      properties:
        count:
          format: int64
          type: integer
        entity_type:
          format: int32
          type: integer
      required:
        - entity_type
        - count
      type: object
    ProbableRefItem:
      additionalProperties: false
      properties:
        created_at:
          format: date-time
          type: string
        dead_at:
          format: date-time
          type:
            - string
            - "null"
        entity:
          $ref: "#/components/schemas/EntitySummary"
        entity_id:
          format: int64
          type: integer
        entity_type:
          format: int32
          type: integer
        external_id:
          type: string
        link_kind:
          format: int32
          type: integer
        matched_by:
          type: string
        source:
          $ref: "#/components/schemas/CatalogSource"
        source_id:
          format: int32
          type: integer
        verified_at:
          format: date-time
          type:
            - string
            - "null"
        verified_by:
          format: int64
          type:
            - integer
            - "null"
      required:
        - entity
        - entity_type
        - entity_id
        - source_id
        - external_id
        - link_kind
        - matched_by
        - verified_by
        - verified_at
        - created_at
        - dead_at
      type: object
    ProposalActionData:
      additionalProperties: false
      properties:
        action:
          type: string
        execute_after:
          format: date-time
          type: string
        id:
          format: int64
          type: integer
      required:
        - id
        - action
      type: object
    ProposalActionInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/ProposalActionInputBody.json
          format: uri
          readOnly: true
          type: string
        reason:
          description: reject only
          type: string
      type: object
    ProposalItem:
      additionalProperties: false
      properties:
        entity_type:
          format: int32
          type: integer
        execute_after:
          format: date-time
          type:
            - string
            - "null"
        executed_at:
          format: date-time
          type:
            - string
            - "null"
        field_resolution:
          contentEncoding: base64
          type: string
        id:
          format: int64
          type: integer
        note:
          type: string
        proposed_at:
          format: date-time
          type: string
        proposed_by:
          format: int64
          type: integer
        source:
          $ref: "#/components/schemas/EntitySummary"
        source_entity_id:
          format: int64
          type: integer
        status:
          format: int32
          type: integer
        target:
          $ref: "#/components/schemas/EntitySummary"
        target_entity_id:
          format: int64
          type: integer
      required:
        - source
        - target
        - id
        - entity_type
        - source_entity_id
        - target_entity_id
        - status
        - field_resolution
        - proposed_by
        - proposed_at
        - execute_after
        - executed_at
        - note
      type: object
    QueueSummary:
      additionalProperties: false
      properties:
        candidates:
          items:
            $ref: "#/components/schemas/CandidateBucketCount"
          type:
            - array
            - "null"
        probable_refs:
          items:
            $ref: "#/components/schemas/ProbableRefBucketCount"
          type:
            - array
            - "null"
      required:
        - candidates
        - probable_refs
      type: object
    RefActionData:
      additionalProperties: false
      properties:
        done:
          type: boolean
      required:
        - done
      type: object
    RefKeyBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/RefKeyBody.json
          format: uri
          readOnly: true
          type: string
        entity_id:
          format: int64
          type: integer
        entity_type:
          format: int32
          type: integer
        external_id:
          minLength: 1
          type: string
        source_id:
          format: int32
          type: integer
      required:
        - entity_type
        - entity_id
        - source_id
        - external_id
      type: object
    RejectRefInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/RejectRefInputBody.json
          format: uri
          readOnly: true
          type: string
        entity_id:
          format: int64
          type: integer
        entity_type:
          format: int32
          type: integer
        external_id:
          minLength: 1
          type: string
        reason:
          description: Why the pairing is wrong — recorded as permanent negative knowledge
          minLength: 1
          type: string
        source_id:
          format: int32
          type: integer
      required:
        - entity_type
        - entity_id
        - source_id
        - external_id
        - reason
      type: object
    ReleaseWorkData:
      additionalProperties: false
      properties:
        status:
          format: int32
          type: integer
        work_id:
          format: int64
          type: integer
      required:
        - work_id
        - status
      type: object
    ReleaseWorkInputBody:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/ReleaseWorkInputBody.json
          format: uri
          readOnly: true
          type: string
        note:
          type: string
        work_id:
          description: The quarantined work to release to live
          format: int64
          minimum: 1
          type: integer
      required:
        - work_id
      type: object
    StaffClaimActionRequest:
      additionalProperties: false
      properties:
        $schema:
          description: A URL to the JSON Schema for this object.
          examples:
            - https://example.com/StaffClaimActionRequest.json
          format: uri
          readOnly: true
          type: string
        reason:
          description: Moderator note; REQUIRED for decline, recorded on the event
          type: string
      type: object
info:
  title: KUN Catalog Admin API
  version: 1.0.0
openapi: 3.1.0
paths:
  /api/v1/admin/catalog/candidates:
    get:
      operationId: listCatalogCandidates
      parameters:
        - description: 0=pending 1=accepted 2=rejected 3=deferred; -1 = all
          explode: false
          in: query
          name: status
          schema:
            default: -1
            description: 0=pending 1=accepted 2=rejected 3=deferred; -1 = all
            format: int32
            type: integer
        - description: -1 = all
          explode: false
          in: query
          name: entity_type
          schema:
            default: -1
            description: -1 = all
            format: int32
            type: integer
        - description: 0=shared_external_id 1=name_norm_equal 2=name_fuzzy 3=importer_suggest 4=llm_suggest; -1 = all
          explode: false
          in: query
          name: reason
          schema:
            default: -1
            description: 0=shared_external_id 1=name_norm_equal 2=name_fuzzy 3=importer_suggest 4=llm_suggest; -1 = all
            format: int32
            type: integer
        - description: 1-based page number
          explode: false
          in: query
          name: page
          schema:
            description: 1-based page number
            format: int64
            type: integer
        - description: Items per page (max 200)
          explode: false
          in: query
          name: limit
          schema:
            description: Items per page (max 200)
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopePageCandidateItem"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: List match candidates (the ambiguity bucket)
      tags:
        - catalog-admin
  /api/v1/admin/catalog/candidates/decide:
    post:
      operationId: decideCatalogCandidate
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DecideCandidateInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeDecideCandidateData"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: "Decide a match candidate: accept (credit_name → link a person; else opens a merge proposal), reject (kept forever) or defer"
      tags:
        - catalog-admin
  /api/v1/admin/catalog/candidates/summary:
    get:
      operationId: catalogQueueSummary
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeQueueSummary"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: "Queue depth: candidates grouped by entity type × status, probable refs grouped by entity type"
      tags:
        - catalog-admin
  /api/v1/admin/catalog/claims/pending:
    get:
      operationId: listCatalogPendingClaims
      parameters:
        - description: Restrict to one claiming site
          explode: false
          in: query
          name: site
          schema:
            description: Restrict to one claiming site
            type: string
        - description: Items per page (max 200)
          explode: false
          in: query
          name: limit
          schema:
            description: Items per page (max 200)
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopePagePendingClaimItem"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: "The claim review queue: submissions awaiting a decision, oldest submission first"
      tags:
        - catalog-admin
  /api/v1/admin/catalog/claims/{id}/{action}:
    post:
      operationId: actOnCatalogClaimAsStaff
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            minimum: 1
            type: integer
        - description: approve | decline | ban | unban
          in: path
          name: action
          required: true
          schema:
            description: approve | decline | ban | unban
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StaffClaimActionRequest"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeClaimActionResult"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Approve / decline / ban / unban a claim (decline and ban record the reason on the event)
      tags:
        - catalog-admin
  /api/v1/admin/catalog/image-references:
    get:
      operationId: listCatalogImageReferences
      parameters:
        - description: The image's sha-256 (64 hex characters)
          explode: false
          in: query
          name: hash
          schema:
            description: The image's sha-256 (64 hex characters)
            type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeImageReferencesData"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: List the catalog rows that reference an image hash (the pre-delete check)
      tags:
        - catalog-admin
  /api/v1/admin/catalog/image-references/detach:
    post:
      operationId: detachCatalogImageReferences
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DetachImageReferencesInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeDetachImageReferencesData"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Release every catalog reference to an image hash (run before deleting the bytes)
      tags:
        - catalog-admin
  /api/v1/admin/catalog/names/detach:
    post:
      operationId: detachCatalogName
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DetachNameInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeDetachNameData"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Detach a credit name from its person (reverses a person link; removes the person if it empties)
      tags:
        - catalog-admin
  /api/v1/admin/catalog/proposals:
    get:
      operationId: listCatalogProposals
      parameters:
        - description: 0=open 1=approved 2=executed 3=rejected 4=withdrawn; -1 = all
          explode: false
          in: query
          name: status
          schema:
            default: -1
            description: 0=open 1=approved 2=executed 3=rejected 4=withdrawn; -1 = all
            format: int32
            type: integer
        - description: -1 = all
          explode: false
          in: query
          name: entity_type
          schema:
            default: -1
            description: -1 = all
            format: int32
            type: integer
        - explode: false
          in: query
          name: page
          schema:
            format: int64
            type: integer
        - explode: false
          in: query
          name: limit
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopePageProposalItem"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: List merge proposals
      tags:
        - catalog-admin
  /api/v1/admin/catalog/proposals/{id}/{action}:
    post:
      operationId: actOnCatalogProposal
      parameters:
        - in: path
          name: id
          required: true
          schema:
            format: int64
            type: integer
        - in: path
          name: action
          required: true
          schema:
            enum:
              - approve
              - reject
              - withdraw
              - execute
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProposalActionInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeProposalActionData"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Approve / reject / withdraw / execute a merge proposal (execute enforces the 48h cooling-off)
      tags:
        - catalog-admin
  /api/v1/admin/catalog/refs/confirm:
    post:
      operationId: confirmCatalogRef
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefKeyBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeRefActionData"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Promote a probable external ref to exact (409 when the exact slot is taken)
      tags:
        - catalog-admin
  /api/v1/admin/catalog/refs/probable:
    get:
      operationId: listCatalogProbableRefs
      parameters:
        - description: -1 = all
          explode: false
          in: query
          name: source_id
          schema:
            default: -1
            description: -1 = all
            format: int32
            type: integer
        - description: -1 = all
          explode: false
          in: query
          name: entity_type
          schema:
            default: -1
            description: -1 = all
            format: int32
            type: integer
        - explode: false
          in: query
          name: page
          schema:
            format: int64
            type: integer
        - explode: false
          in: query
          name: limit
          schema:
            format: int64
            type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopePageProbableRefItem"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: List unconfirmed probable external refs (the confirmation bucket; includes merge-demoted exacts)
      tags:
        - catalog-admin
  /api/v1/admin/catalog/refs/reject:
    post:
      operationId: rejectCatalogRef
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RejectRefInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeRefActionData"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: "Reject a wrong external ref: deletes it and records first-class negative knowledge (reason required)"
      tags:
        - catalog-admin
  /api/v1/admin/catalog/works/release:
    post:
      operationId: releaseCatalogWork
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReleaseWorkInputBody"
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnvelopeReleaseWorkData"
          description: OK
        default:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HouseError"
          description: Error
      summary: Release a quarantined work to live (the reject-without-merge exit of the mint gate)
      tags:
        - catalog-admin
